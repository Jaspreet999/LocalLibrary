extend layout 

block content

    h1 #{book_instance.book.title}

    p #[strong Title:]
        a(href=book_instance.book.url) #{book_instance.book.title}
    p #[strong Imprint:] #{book_instance.imprint}

    p Do you really want to delete this book instance?

    form(method="POST" action="")
        div.form-group
            input#bookinsatnceid.form_control( type="hidden", value = book_instance._id ,required = true, name="bookinstanceid")

        button.btn.btn-primary(type='submit') Delete    