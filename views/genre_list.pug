extend layout 

block content 

    h1= title

    ul
        if list_item 
            each genre in list_item 
                li 
                    a(href=genre.url) #{genre.name}

        else 
            li There is no genre of books            